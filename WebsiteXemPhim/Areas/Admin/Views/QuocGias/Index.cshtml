@model IEnumerable<WebsiteXemPhim.Models.QuocGia>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Danh sách các quốc gia</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">Thêm quốc gia</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>Tên quốc gia</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.TenQuocGia)
                </td>
                <td class="col-md-3" style=" text-align:right;">
                    <a class="btn btn-success" asp-action="Edit" asp-route-id="@item.QuocGiaId">Sửa</a> |
                    <a class="btn btn-info" asp-action="Details" asp-route-id="@item.QuocGiaId">Chi tiết</a> |
                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.QuocGiaId">Xoá</a>
                </td>
            </tr>
        }
    </tbody>
</table>
